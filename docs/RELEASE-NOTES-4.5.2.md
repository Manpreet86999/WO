# Body OS 4.5.2 — Legacy update compatibility

- Fixes update loops for users on Workout OS 2.x, including 2.5.0.
- Uses the legacy installer filename recognised by those clients while installing the current Body OS application.
- Keeps the stable installer identity and the user’s existing install folder.
- Retains the single-file installer, embedded requirements check, package installation, safety backup, server restart, and post-update notes.

Publish with tag `v4.5.2` and upload **only** `WorkoutOS-Setup.exe`.
